# CLAUDE.md

Canonical user instructions live in `~/AGENTS.md`.

Read `~/AGENTS.md` first. Then read only the relevant files under `~/agent-rules/` for the current task. Do not load all rule modules by default.

Keep this file thin. Put shared behavior in `~/AGENTS.md` and `~/agent-rules/`. Use installed skills for heavier workflows instead of duplicating them here.
